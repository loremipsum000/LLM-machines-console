# Inference Core validation register

The contract revisions under `contract-revisions/` bind accepted package
trees to their generated route and repository baselines. This register tracks
the human-readable gate state without replacing those machine-verifiable
artifacts.

| Package | Scope | Validation state |
| --- | --- | --- |
| PR-02 through PR-07 | Accepted integration history | Bound by reviewed per-package decisions and contract revisions |
| PR-08 | Firecrawl search and static scrape | Passed on the reviewed PR-08 candidate; PR-12 runtime gates retained |
| PR-09 | Activity, audit, and observability | Passed on the reviewed PR-09 candidate; runtime delivery remains PR-12 |
| PR-10 | Lifecycle snapshot and restore foundation | Passed on the reviewed PR-10 candidate; configured adapters and runtime qualification remain PR-12 |
| PR-10C | Emergency isolation | Passed on the reviewed PR-10C source candidate; live topology and runtime qualification remain PR-12 |
| PR-11 | Retained Console information architecture | Passed on the reviewed PR-11 source candidate; deployment and runtime qualification remain PR-12 |
| PR-11A R1-C0 | Product authority and governance correction | Source validation passed and merged through PR 13; aggregate R1-V1 review remains pending; PR-11A is unaccepted and not revision-bound |
| PR-11A R1-S1 | Opaque Console session hardening | Full source and fresh-clone validation plus independent review passed at reviewed head `28655df1a919757329a1493bea5811f064d143d0`; merged through PR 14 at integration commit `39057332207cca6193495453b7336eda07608255`; aggregate R1-V1 and Q0 runtime qualification remain pending; PR-11A is unaccepted and not revision-bound |
| PR-11A R1-E1 | Mandatory core Product edge | Local and clean detached fresh-clone full source validation plus independent review passed at `c60280c11318aa21d230e7002cb7d703625a7168`; merged through PR 15 at integration commit `1743cb746f87c7497a34f4de7e3bfc0db3ff0be2`; aggregate R1-V1 and Q0 runtime qualification remain pending; PR-11A is unaccepted and not revision-bound |
| PR-11A R1-K1 | Signing custody and public trust | Local and clean detached fresh-clone full source validation plus independent review passed at `cd1f7c43bd7c2abe32a2423d1ce77506ecde84cc`; merged through PR 16 at integration commit `ffc49eb6e97169ced202efbaa6363c85bfdd40dc`; aggregate R1-V1 and Q0 runtime qualification remain pending; PR-11A is unaccepted and not revision-bound |
| PR-11A R1-D1 | Storage, backup, retention, and recovery | Local and clean detached fresh-clone full source validation plus independent review passed at `46295906c3d733b0e56abe94d9732d8eb0549c29`; merged through PR 17 at `cc08cf8e9afce12def143f2f395d30bfbe04f515`; the D1-owned hygiene successor passed clean detached source validation and merged through PR 18 at `949d1f2fd05a8329e7676ad2423bf55d0eab29ba`; aggregate R1-V1 and Q0 runtime qualification remain pending; PR-11A is unaccepted and not revision-bound |
| PR-11A R1-H1 | Repository-wide Biome hygiene successor | Exact 32-path hygiene delta and source-only successor contract passed local and clean detached full source validation plus independent review at `49ad418408aab32f30e7f6008aa71ad66ba5e708`; PR-11A is unaccepted, not revision-bound, and not runtime-qualified |
| PR-11A R1-V1 successor | Aggregate source evidence, inactive contract revision, and source-closure binding | Exact post-H1 input `0e794ccd6e0f2593f2e6ab34b8fc1d521835b6fd`; full local and clean detached validation plus independent review passed for candidate `fb5ac5be13012a3de5e2da733bdc83c7d82efed3`; PR 20 merged it at `837c3c3e139fd6b82db650d20a4f0fcf902e2fda`; the merge tree equals the validated candidate tree and the diff is empty; PR-11A is source-closed and revision-bound to that integration commit; accepted remains false; runtimeQualified remains false; contract activation remains inactive; Q0 remains NOT_STARTED; Grafana qualification remains NOT_STARTED; PR-12 subsequently entered its separately governed source workflow |
| PR-12 source closure | Aggregate deterministic packaging evidence and amended exact release-source binding | PR 33 through PR 39 remain immutable evidence. Amendment 1 remains immutable evidence. Amendment 2 remains immutable evidence. Amendment 3 remains immutable evidence. Amendment 4 remains immutable evidence. PR 53 remains protected functional history. PR 54 repaired the stale Firecrawl API build-test list, PR 55 corrected the unavailable Node API build input, and PR 57 corrected the index-selected Golang, Wolfi, and Playwright linux/amd64 identities. The first actual image then demonstrated the Koffi 2.9.0 executable-stack incompatibility on Wolfi. PR 59 corrected only the exact native-module ELF stack flag, added a final-runtime load check, regenerated deterministic source evidence, passed complete local and detached validation plus material review, and merged at `eefb9c3eb372a5b6789223458ccc319fa9784a04` with identical candidate tree `884965cf79cfa77044c962614e54a8decfc2b1fd`. Amendment 5 binds that merge as the current D2A release source. The prior `4b2fc6a`, `059866d`, and `5e7761b` release sources are historical, not current. Accepted remains false; runtimeQualified remains false; contract activation remains inactive; D2A-RC and Q0 remain NOT_STARTED; no deployment or Product main transition occurred. |
| F0-P1 | Disposable PostgreSQL persistence and restart qualification | Local source candidate applies the real migration, runs actual Web/BFF with durable repositories, preserves intended metadata and encrypted sessions across one BFF restart, fails database readiness closed, proves recovery and retention canaries, and removes all disposable state; exact-Core and runtime qualification remain pending |
| F0-O1 | Retained Console observability projection | Local browser candidate proves Admin and Operator read-only projection parity, curated LiteLLM and hardware data, exact private GET-only source routes, controlled outage recovery, retention canaries, and absence of native administration and Grafana; real-service and runtime qualification remain pending |
| F0-I1 | Disposable Keycloak identity integration | Local browser candidate uses exact Keycloak 26.7.0 for password and TOTP login, Admin and Operator role mapping, identity-cookie isolation, native-admin denial, and Console logout; synchronized-clock refresh, reuse, concurrency, outage, commissioning, FGAP mutations, exact-Core, and runtime qualification remain pending |
| F0-I2 | Scoped Console Team identity mutation | Local browser candidate uses exact Keycloak 26.7.0 plus disposable PostgreSQL to prove Admin-only user creation, canonical Operator assignment, one-time local-password generation and rotation, disable/reactivate, durable journal and metadata-only audit, Operator read-only behavior, and denial of realm/client/native-admin authority; commissioning, exact-Core, backup/restore, and runtime qualification remain pending |
| F0-L2 | Private LiteLLM integration | Local browser candidate uses exact LiteLLM v1.85.0 between the Product gateway and deterministic inference, proves streaming and non-streaming Application traffic, real read-only Console projections, controlled outage recovery, internal routing-key separation, native-access denial, and zero-content-retention canaries; SGLang, capacity, exact-Core, and runtime qualification remain pending |
| F0-F2R | Native-Linux Firecrawl runtime correction | Candidate must prove that a restrictive umask cannot make the generated allowlist unreadable to non-root Squid; preserve the exact two-host allowlist and loopback-only API and Firecrawl authorities; run actual reduced Firecrawl search, static scrape, Application credential lifecycle, isolation, egress denial, unsupported-route denial, zero-retention canaries, and cleanup on native Linux/amd64; preserve historical F0-F2 evidence byte-for-byte; and pass focused, complete, detached-clone, security, and material-review gates without new credentials, broader egress, licensing changes, Product acceptance, or runtime qualification. |
| F0-C1 | Integrated reduced-Core startup | Local integrated candidate starts actual Web/BFF, PostgreSQL, Keycloak, private LiteLLM, reduced Firecrawl, and retained observability with deterministic inference; clean initialization, reduced Product flows, restart persistence, no-bypass checks, retention canaries, and teardown pass; exact-Core, real SGLang, and runtime qualification remain pending |
| F0-SG1 | Actual SGLang internal integration smoke | Exact SGLang 0.5.13 and exact LiteLLM v1.85.0 pass private streaming and non-streaming Chat Completions, token accounting, wrong-model and over-context denial, controlled upstream outage and same-image recovery, LAN denial, and zero-content canaries on authorized internal XPU test hardware; capacity remains unmeasured, the smoke is not a delivery profile, and production hardware qualification remains pending |
| F0-V1 | Pre-Genesis aggregate functional closure | Candidate must pass the focused topology and boundary suite, complete root gate, Biome, typecheck, production build, integrated reduced-Core browser/API lane, scoped Team identity lane, source security scans, clean detached-clone replay, and independent material review; the protected integration tree must equal the reviewed candidate tree before it can be named as the pre-Genesis source candidate; Genesis publication, Product acceptance, runtime qualification, contract activation, and Q0 remain outside this package |
| F0-UAT0 | Persistent founder evaluation environment | Candidate must prove an explicit start, credential-free status, and explicit stop contract; native Linux/amd64 and loopback-only authority enforcement; mode-`0600` generated credential custody; reuse of F0-C1 service and cleanup ownership; complete source gates and detached validation. Actual VM startup and the uninterrupted customer journey remain F0-E2E2 evidence; Genesis, release, deployment, Product acceptance, runtime qualification, and Q0 remain outside this package |
| F0-E2E2 | Exact founder customer journey | Candidate must execute the pinned standard OpenAI Node SDK in a separate process with the one-time Application credential passed only through bounded stdin; prove model discovery plus streaming and non-streaming Chat Completions through the TLS-verified API authority; preserve the complete actual Console, Keycloak, private LiteLLM, Firecrawl, PostgreSQL, observability, lifecycle, isolation, no-bypass, and zero-retention journey; pass complete source gates and a protected VM117 replay before F0-UX2. Genesis, Product acceptance, runtime qualification, D2A, and Q0 remain outside this package |
| F0-V2 | Founder handover aggregate closure | Candidate must bind protected PRs 65 through 74; rerun the complete root gate, Biome, typecheck, production build, source security scans, and detached-clone replay; prove the persistent private linux/amd64 environment reaches READY only after the actual Console, Keycloak, PostgreSQL, LiteLLM, reduced Firecrawl, observability, browser, external OpenAI SDK, restart, no-bypass, and retention checks pass; and pass a final material review. The protected integration tree must equal the reviewed candidate tree before it can be proposed as the Genesis source snapshot. The environment remains running for founder testing. Product acceptance, runtime qualification, contract activation, Genesis publication, D2A, Q0, Product main, and the separate cross-host SGLang composite subgate remain outside this package. |
| F0-N0 | Prospective retained native administration scope correction | Candidate must bind the exact protected input and corrected reduction boundary, preserve historical records unchanged, enumerate every current source conflict and owning successor, keep all native routes inactive, retain direct-port denial and Console primacy, and pass focused, complete, detached-clone, security, and material-review gates. It does not admit a service version, activate a route, deploy, publish Genesis, qualify runtime, or accept the Product. |
| F0-N1 | LiteLLM OSS-only downstream and native-role characterization | Candidate must reproduce the exact sanitized source and deterministic linux/amd64 image twice; prove the upstream signature and MIT source identity; prove Enterprise material absent from source, installed environment, OCI filesystem, runtime imports, SBOM, and notices; bind SBOM, vulnerability, license, and transitive corresponding-source requirements; migrate v1.85.0 state; prove Keycloak Authorization Code plus PKCE with no license material, two of five billable users, Admin `proxy_admin`, Operator `internal_user` own-key and own-spend authority, cross-user and global mutation denial, native logout, controlled identity outage, restart, routing, accounting, retention, and loopback-only characterization. The exact Product-edge route remains inactive pending F0-N5. |
| F0-N2 | Grafana 13.1.3 Admin-only native access and security admission | Candidate must bind the exact official tag, peeled source commit, linux/amd64 OCI index, manifest, image configuration, AGPL license text, and policy-fresh Trivy scan; disable the unused Elasticsearch, Tempo, and Zipkin datasource plugins; prove those plugins are absent from runtime inventory and return 404; prove Editor cannot create a datasource; prove Authorization Code plus PKCE, Admin Editor, Operator/mixed/unknown denial, no server administrator, native session and logout, restart, controlled identity outage, cross-origin mutation denial, loopback-only access, and cleanup. The bounded vulnerability disposition expires on 2026-09-12. Product-edge activation and the required HTTPS browser replay remain F0-N5. |
| F0-N3 | Keycloak 26.7.0 theme, password session profile, and scoped appliance-realm administration | Candidate must exact-bind the Keycloak image and 25-file theme; prove Authorization Code plus PKCE, password-only login, 8-hour idle and 24-hour maximum sessions, independent native cookies, Admin-only user/password/session operations, metadata-only Admin subject binding, Operator denial, cross-realm and administrative escalation denial, native logout, restart and outage behavior, credential-free route inventory, cleanup, complete source gates, detached replay, and material review. Users `manage` deletion remains an accepted upstream residual that blocks activation until F0-N5 enforces the exact Product-edge DELETE denial. Product acceptance, runtime qualification, Genesis, and Q0 remain pending. |
| F0-N3R | Console session contract correction | Candidate must bind the exact protected input; update only the stale shared 30-minute/8-hour contract and its directly dependent tests and current registers to the approved 8-hour/24-hour profile; preserve five-minute access tokens, refresh rotation, revocation, same-origin return handling, identity-outage recovery, concurrent refresh, and logout; prove idle and maximum expiry with controlled time or session timestamps; keep historical F0-N3 evidence byte-identical; bind the historical F0-N6 path inventory to its exact admitted candidate instead of the moving worktree; and pass focused, complete, detached-clone, security, and material-review gates without changing runtime implementation or Product boundaries. |
| F0-N3T | Keycloak runtime-bootstrap correction | Candidate must bind exact protected input `155a4fe5004b7a0d0e2ae880aea752e69deeafd2`; keep the generated credential-bearing host state root at `0700` while explicitly rendering the Keycloak container import directory and realm file readable under restrictive umasks; preserve the global `offline_access` role and scope but exclude them from the realm default role and Console optional scopes; synchronize controlled time before real Keycloak login; prove the approved password-only flow without TOTP; preserve historical F0-I2, F0-N3, and F0-N3R evidence byte-for-byte; pass exact Keycloak 26.7.0 plus PostgreSQL 17.6 browser proof on isolated VM117; remove all run-owned state; and pass focused, complete, detached-clone, security, and material-review gates without changing Product runtime behavior, authority, route, or permissions. |
| F0-L2R | Integrated LiteLLM OSS runtime binding | Candidate must bind exact protected input `f8ac8d762ff2838937c46e4826c5faeeb53a0ab5`; verify exact admitted LiteLLM OSS `v1.96.2-llmm.1` image ID, manifest, platform, source revision, and labels before execution; reject LiteLLM 1.85, upstream mutable images, and fallback substitution; preserve generated secret custody while making only credential-free non-root service fixtures readable under restrictive umasks; pass the full reduced-Core VM117 path for OIDC, model discovery, streaming and non-streaming inference, accounting, Firecrawl, observability, restart persistence, no-bypass, cleanup, and zero-content retention; preserve the founder environment and earlier local candidates; and pass focused, complete, detached-clone, security, and material-review gates. Product behavior and native ingress remain unchanged; activation and three-service browser qualification remain F0-N7 gates. |
| F0-L2S | LiteLLM immutable-subject native commissioning successor | Candidate must bind exact protected input `9adf82ffbf8f4617a7846e69e436acd21b9270c8`; validate exact admitted OSS image identity and labels before execution; use Keycloak `sub` as both LiteLLM `user_id` and `sso_user_id`; commission only the approved Admin `proxy_admin` and Operator `internal_user` mappings over a private service URL with the root-only LiteLLM master secret; create no virtual key, delete no user, return no credential material, and fail closed on public or unreviewed hosts; prove create-then-no-op idempotence, post-restart persistence, browser OIDC plus PKCE, own-key and spend boundaries, global and cross-user denial, logout, outage, routing, accounting, retention, cleanup, and founder-environment preservation on VM117; preserve historical evidence; and pass focused, complete, detached-clone, security, and material-review gates. Native ingress and F0-N7 aggregate qualification remain pending. |
| F0-N4 | Portainer upstream-security deferral | Candidate must bind protected input `2193952c191dc0db7a2f0d5a0072e63e30d8c0ad`, preserve F0-N0 and all prior records byte-for-byte, record the exact Portainer CE 2.39.6 feasibility identities and blocking security disposition without admitting an image, keep Portainer absent from the current Core BOM, immutable image lock, startup, ingress, navigation, deployment, and advertising, forbid a broad downstream fork or unsafe bridge, and bind future reconsideration to a separately authorized F0-N4R against a suitable newer upstream CE release. The amendment changes no Product behavior or runtime configuration and permits only the three-service F0-N5 through F0-N8 sequence to proceed. |
| F0-N5 | Three-service native Product-edge profiles | Candidate must preserve the F0-E0 four-authority core, add only the exact Grafana, LiteLLM, and appliance-realm Keycloak dedicated hosts and fixed private upstreams, bind the characterized native routes and query keys, preserve each service's own OIDC session, reject Console cookies/tokens and Product credentials, deny WebSocket upgrades, enforce Host/SNI and unsafe-path controls, return 403 for exact Keycloak user DELETE, keep Portainer and all other native services absent, and preserve metadata-only logging. Complete focused, root, detached-clone, source-security, and material-review gates must pass. HTTPS browser roles, outage, restart, retention, and no-bypass proof remain F0-N7; activation, runtime qualification, Product acceptance, Q0, and Genesis remain pending. |
| F0-N5R | Keycloak dual-authority edge correction | Candidate must bind exact protected input `4585830069cb91cf1806a3a3308c7663860b6822`; preserve historical F0-N5 evidence byte-for-byte; configure Keycloak 26.7.0 with identity frontend and dedicated `/keycloak` admin authorities; keep the upstream at root; normalize `/keycloak` only inside exact allowlisted admin locations after raw-path rejection; keep normal OIDC on the identity host; preserve native cookies, redirects, PKCE, Origin, CSRF, and logout; deny duplicate admin-host OIDC and exact user DELETE; prove the rendered Nginx syntax and bounded path behavior on disposable linux/amd64; clean all owned state; and pass focused, complete, detached-clone, security, and material-review gates. Complete three-service browser and no-bypass qualification remains F0-N7. |
| F0-N5S | Grafana OAuth entry query correction | Candidate must bind exact protected input `dbdc1005711ea2cbfb3658a268181dbd2deef6e0`; preserve historical F0-N5 and F0-N5R evidence byte-for-byte; add a Grafana-specific query discriminator that accepts no query for `GET` or `HEAD /login/generic_oauth` initiation and accepts only `code`, `iss`, `session_state`, and `state` for callback; leave LiteLLM's callback discriminator unchanged; reject unknown query keys; prove PKCE, Admin Editor, Operator denial, no server administrator, dashboard mutation, datasource denial, credential-free evidence, and complete cleanup through HTTPS on disposable linux/amd64; and pass focused, complete, detached-clone, security, and material-review gates. Complete three-service browser and no-bypass qualification remains F0-N7. |
| F0-N5T | Keycloak Admin browser token Origin correction | Candidate must bind exact protected input `ec2508c76f2b35b34407738dd2f3cdcc286e4608`; preserve historical F0-N5, F0-N5R, and F0-N5S evidence byte-for-byte; permit an absent Origin for supported server-side human-realm token exchange; forward only the exact dedicated Keycloak Admin Origin; reject every other browser Origin with `403` before Keycloak; keep Authorization, Cookie, and Console session material stripped; prove exact Keycloak 26.7.0 Admin Console token exchange, foreign-Origin denial, no client-policy broadening, credential-free evidence, and complete VM117 cleanup; and pass focused, complete, detached-clone, security, and material-review gates. Native activation and complete three-service qualification remain F0-N7 gates. |
| F0-N5U | Keycloak session identifier edge correction | Candidate must bind exact protected input `fbcc7d81bef80c0346942380a0361fe64c2b69fa`; preserve historical F0-N5 through F0-N5T evidence byte-for-byte; bind the exact Keycloak 26.7.0 tag commit and source hashes proving 18-byte URL-safe Base64 user-session identifiers; allow only 24 characters in `A-Za-z0-9_-` on the exact session `DELETE` path; reject malformed, UUID-shaped, encoded-traversal, wrong-method, and query-bearing requests before upstream; preserve the user-delete `403`, native Authorization, and Console-token separation; prove real invalidation and cleanup on isolated VM117; and pass focused, complete, detached-clone, security, and material-review gates. Complete three-service qualification remains F0-N7. |
| F0-N5V/F0-N1 | LiteLLM native cookie security correction | Candidate must bind exact protected input `0317d2effb29a1a6cbaa4fc0fc8332b140a5a03f`; preserve historical F0-N1 and F0-N5 through F0-N5U evidence byte-for-byte; bind exact LiteLLM OSS v1.96.2 source showing the native UI reads `token` through JavaScript; force Secure and SameSite=Lax on `litellm_cp_return_to`, `litellm_oauth_state`, `sso_state`, and `token`; force HttpOnly on the three state cookies while keeping `token` non-HttpOnly; reject policy weakening and unexpected cookies; preserve the dedicated HTTPS authority, exact route allowlist, private direct port, and Console-token separation; prove Admin and Operator OIDC, PKCE, role, cookie, logout, credential-free evidence, founder-environment preservation, and cleanup on isolated VM117; and pass focused, complete, detached-clone, security, and material-review gates. Complete three-service qualification remains F0-N7. |
| F0-N6 | Console Technical Tools navigation and support-boundary copy | Candidate must keep Technical Tools inside Settings; server-filter Admin to Grafana, LiteLLM, and Keycloak and Operator to LiteLLM only; keep Portainer absent; derive credential-free HTTPS links from exact commissioning hostname inputs and fixed paths; reject unsafe or malformed inputs without emitting a link; open native tools outside Console without forwarding Console session material or Product credentials; and distinguish Console Application credentials from advanced LiteLLM virtual keys. Focused Web tests, the complete Product gate, Biome, typecheck, production build, clean detached replay, security scans, and material review must pass. Native ingress and runtime remain inactive pending F0-N7. |
| F0-N7 | Aggregate retained native-access validation | Candidate must bind exact protected input `a714672c91fc512e075e93dd084a88a26e77c22a`; use exact Grafana 13.1.3, LiteLLM OSS `v1.96.2-llmm.1`, Keycloak 26.7.0, PostgreSQL 17.6, Product edge, and a real browser on isolated VM117; prove Admin and Operator role boundaries, native sessions, Authorization Code plus PKCE, logout, restart, identity and service outage, Keycloak subject binding and user-delete denial, LiteLLM own-key isolation, exact native cookie policy, direct-port and alternate-host denial, forwarding-header normalization, unsafe-route and traversal denial, no Console-token forwarding, zero workload-content retention, founder-environment preservation, and complete owned cleanup. The conditional `litellm_cp_return_to` policy must remain bound without allowing `return_to` on the approved query-free login route. Portainer and retired Product surfaces remain absent. Complete local and detached source gates plus material review must pass. Product acceptance and runtime qualification remain false, activation remains pending F0-N8 and deployment, Q0 remains NOT_STARTED, and Genesis remains unpublished. |
| F0-N8 | Retained native-access pre-Genesis closure | Candidate must bind exact protected input `67707a8cb14256f8a02f3c2bf20b539dfa7a059f` and tree `ac74faf1c4ba394661cdf642ba0c3e80a0ddaa44`; prove the exact first-parent PR 90 through PR 107 history, candidate-to-integration tree equality, and byte-exact evidence fingerprints for every admitted native package; preserve F0-F2R as unrelated intervening protected history; bind the corrected three-service roles, Product-edge and session separation, no-bypass and retention results, retired-surface absence, and Portainer deferral; change only the five declared governance paths; and pass focused, complete, detached-clone, security, and material-review gates. It must not change Product behavior or runtime, activate ingress, touch VM103, publish Genesis, start D2A, signing, or Q0, or alter Product main. Deployment and publication require separate approval. |
| Fable audit remediation | Governance-only closure after PR 148 through PR 150 | Binds audited PR 147 candidate `0f0399c6a091d4ec0ea78f4379362add38c87b22` and tree `ced785bf0d7f63efab780443e1207b0e3ece73d8`, the three tree-identical protected remediation merges, independent material reviews, and exact validation-log hashes. SEC-001, SEC-003, BUG-001 through BUG-003, ARCH-001, MAINT-002, and MAINT-003 are fixed. SEC-002 is accepted risk; performance items await measurement; ARCH-002, MAINT-001, and H-1 through H-4 remain qualification or future-maintenance work. Product acceptance and runtime qualification remain false, contract activation remains inactive, Genesis remains unpublished, and the external audit clone remains preserved. |
| Fable business and pristine closure | Governance-only closure after PR 152 and PR 153 | Binds the 10-file business-architecture and 13-file code-pristine report inventories to audited PR 147, and binds the two tree-identical protected successors plus exact validation hashes and independent reviews. PG-001, PG-002, AD-001, AD-003, CL-004, CL-008, and CL-012 are fixed. UX-001 is partially fixed: Product Keys now have canonical authority, while copy still needs to distinguish Product Keys, credential identifiers, audit verification keys, and LiteLLM virtual keys. CL-017 is partially fixed: the double navigation mapping and historical marker are gone, while `updateActionEnabled` remains owned by deferred PG-004. Every other report item is explicitly assigned to product-roadmap, runtime-qualification, measurement, research, maintenance, or approval-gated refactor work. Product acceptance and runtime qualification remain false, contract activation remains inactive, Genesis remains unpublished, and the external audit clone remains preserved. |
| Fable business and pristine gate-integrity amendment | Prospective validation correction after PR 156 | Preserves the PR 154 closure unchanged; records that its local root gate contained one Web test failure masked by a non-pipefail `tee` pipeline while its detached replay passed; binds the native-dialog synchronization successor, 50 focused repetitions, exact failure-safe local and detached gate hashes, and independent material review. Product behavior and credential assertions are unchanged. Product acceptance and runtime qualification remain false, contract activation remains inactive, Genesis remains unpublished, and Product main admission remains subject to its retention ruleset. |
| F0-N4R1 | Prospective Portainer CE source-security reassessment | Candidate must bind protected input `7bed599067323429c30ee1d073b9ca22d4c9f867` and tree `3881b27d3425f5cc6bd0f97e2e1b2022c0d0f9e2`; preserve F0-N4 and all historical records byte-for-byte; record that historical raw scanner, SBOM, and OCI artifacts were not located; independently bind exact Portainer CE 2.39.6 source, source-built downstream `2.39.6-llmm.1`, linux/amd64 build inputs, two independently sealed byte-identical assemblies, exact OCI and canonical-inventory digests, A/B environment, build-log, reachability, and seal receipts, evidence tooling, combined and frontend CycloneDX SBOMs, frontend runtime binding, in-toto provenance, raw and normalized Trivy and govulncheck evidence, the complete 38-current-finding matrix and 9 historical remediations, Zlib license, attributions, notices, and complete 581-of-581 frontend and runtime license custody; reject the official image as a downstream substitute; preserve raw scanner counts while proving zero unresolved reachable Critical or High findings under the exact source-only profile; bind the exact post-build Dockerfile configuration scan and carry DS-0002 effective-root and DS-0026 missing-healthcheck findings as activation-blocking F0-N4R2 privilege and health-contract preconditions without changing the image in R1; retain exact external activation preconditions; and pass focused, complete, release, detached-clone, security, license, and material-review gates. It must not add Portainer to the Core BOM or image lock, change startup, ingress, Console, or Product behavior, activate or deploy Portainer, publish Genesis, start Q0, or imply Product or runtime acceptance. Exact artifact and evidence hashes must be bound before admission; placeholders fail closed. |

## PR-08 required evidence

- Exact private source checkpoint and Product base identities remain bound.
- Every reconstructed unit maps through `pr-08-firecrawl-source-manifest.json`
  and one target-file-level row in `source-map.jsonl`; excluded pilot content,
  pilot ancestry, and migration `0027` remain absent.
- Dedicated Firecrawl keys cannot authenticate to inference, and inference,
  OAuth, human, or LiteLLM credentials cannot authenticate to Firecrawl.
- Default-off, disclaimer, Admin-only mutation, Operator read-only, no-expiry,
  86,400-second overlap, immediate revoke, parent-Application disable, and
  passive connection-evidence tests pass. F0-U2 supersedes the earlier current
  role split without rewriting the historical PR-08 evidence.
- Exactly public T2 `POST /v2/search` and `POST /v2/scrape` are admitted while
  the two public inference routes remain unchanged. The six authenticated
  Application admin routes remain control-plane-only. Unsupported methods,
  routes, capability fields, unsafe URLs, redirects, DNS answers, native
  Firecrawl surfaces, direct cloud Firecrawl access, and unapproved egress
  fail closed.
- Request and response bounds, cancellation, timeout, atomic optional rate and
  concurrency protection, failure accounting, and upstream error tests pass.
- Source-level BFF retention canaries remain absent across success, rejection,
  cancellation, timeout, upstream failure, crash, and restart fixtures. Native
  Firecrawl logger and span isolation plus runtime canaries remain PR-12 gates.
- Query-free request logging is bound at the BFF entrypoint. It retains no raw
  request target, query, header, hostname, body, or Firecrawl workload content.
  Unsupported `/v2` pathnames are logged only as `/v2/[unsupported]`.
- Settings accepts only the governed internal upstream
  `http://firecrawl-api:3002`; hosted, HTTPS, alternate-host, and
  alternate-port values fail closed.
- The generic installation profile is present and default off; native service
  ports and state remain private and transient.
- Source provenance, notices, license boundaries, source-level SBOM inputs,
  secrets, credentials, internal topology, CI assumptions, and public
  information scans pass.
- Full typecheck, unit, database, guardrail, formatting, diff, and clean-clone
  verification pass before review.
- No Web production file changes in PR-08; only three exact contract fixture
  tests may change, and the combined Application UI remains PR-11.

Runtime deployment, image and source qualification, signing, the offline
packet, SBOM, corresponding-source delivery, and final release qualification
are PR-12 evidence, not PR-08 evidence.

## PR-11 required evidence

- The exact accepted base is commit
  `6efab17a6f5f6a474a1dfe1444dcdd63e4973dd7`, tree
  `44d6fb34db5f3d35e8b2f9bd2259756aec63b8a8`.
- Every PR-02 through PR-10C revision and evidence file remains immutable. The
  two live register transitions retain their original PR-08 fingerprints from
  the accepted base through an explicit successor-historical binding.
- Global navigation contains exactly the seven accepted surfaces in the fixed
  order, and `/` renders a source-backed Overview rather than redirecting.
- Applications presents inference and Firecrawl together without merging their
  credential namespaces or changing Firecrawl's default-off policy.
- Grafana, LiteLLM, and Keycloak remain reduced previews. No live native href,
  native URL, or expert-session activation is present before PR-12. The exact
  production href manifest, internal-only Overview href schemas, BFF Overview
  response parsing, and null-only expert URL fields fail closed against
  literal, aliased, or BFF-supplied external links.
- `.env.example` differs from the accepted base only by deletion of the exact
  retired `INFERENCE_MODEL_UPDATE_*` block. It has no added lines, retained
  value changes, or unrelated removals, and no other environment path enters
  the package.
- Settings persistence, receipt, and audit tests prove that production
  mutations require PostgreSQL and commit the settings row, idempotency
  receipt, and success audit in one transaction. Missing persistence,
  transaction, or receipt coordination returns unavailable without a false
  success audit; memory state remains fixture-only. Fresh-database telemetry
  preview defaults pass the strict production schema with no fallback data.
- Content security policy tests and production browser gates prove that a
  per-request script nonce reaches Next.js and the response policy unchanged,
  while production `script-src` contains neither `unsafe-inline` nor
  `unsafe-eval`. The route resolver fingerprint transition is exact and
  limited to `apps/web/next.config.ts`, from
  `58f841f6ee4170e90c110e33727d85dabe6a2c096784b05940319d770a958f8b` to
  `79a28582d628e566baa4231d4a718173cf4e9dde14242bc40e214d502262dbb3`.
- Portainer, Agentic, Chat, Knowledge, MCP, Builder, Hub, retired loaders,
  redirects, links, mocks, fixtures, and bundle imports remain outside the
  retained product surface.
- The BFF inventory removes exactly the simulated
  `POST /api/admin/inference/model-updates/apply` route. The resulting 104
  routes include 92 `current-console-seam` routes; no route is added or
  reclassified, and every other classification count and Fastify registrar
  remains unchanged. Public inference, Firecrawl, isolation, authorization,
  and zero-content-retention boundaries remain unchanged.
- The reviewed staged operation policy is exact, source-only, contains no
  deletion, secret, key, runtime, deployment, signing, or vendor-maintenance
  path, and matches the generated source and repository closure deltas.
- The reviewed operation policy binds 2 added and 27 changed source-closure
  paths, 11 added and 54 changed repository paths, no deleted path, and exact
  hashes for 56 changed product-source and test files.
- Full guardrail, Web, BFF, Contracts, database, build, typecheck, formatting,
  security, and clean-clone checks pass before publication.

## Prospective Activity & Audit surface-retirement validation

- Exact six-item navigation and shortcut order are source-tested.
- `/activity` is absent from Web routing, middleware, and Product-edge page
  admission, and explicit no-bypass tests require its denial.
- Overview recent activity remains schema-validated, metadata-only, and
  non-interactive; no response emits an `/activity` href.
- Admin audit export and verification-key controls remain in Settings;
  Operator receives no export control.
- BFF audit read, signed export, verification-key, ingestion, ledger,
  authorization, retention, and content-canary tests remain mandatory.
- Historical route and seven-surface evidence is resolved at its admitted
  commits. The current source successor is 109 routes with 91
  `current-console-seam` routes; the historical route baseline is unchanged.
