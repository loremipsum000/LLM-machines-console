apiVersion: 1
prune: true

datasources:
  - name: LLM Machines Prometheus
    uid: llmm-prometheus
    type: prometheus
    access: proxy
    url: $LLMM_PROMETHEUS_URL
    isDefault: true
    editable: false
    version: 1
    jsonData:
      httpMethod: POST
      timeInterval: 30s
