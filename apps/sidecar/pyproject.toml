[project]
name = "llm-machines-sidecar"
version = "0.1.0"
description = "FastAPI sidecar for the LLM Machines console"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "trafilatura>=2.0.0",
  "uvicorn"
]

[project.optional-dependencies]
dev = [
  "httpx",
  "mypy",
  "pytest",
  "pytest-asyncio",
  "ruff"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/sidecar"]

[tool.pytest.ini_options]
pythonpath = ["src"]

[tool.llm-machines]
tier = "t1"

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.mypy]
python_version = "3.12"
strict = true
